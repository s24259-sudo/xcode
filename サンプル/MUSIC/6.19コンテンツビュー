//
//  ContentView.swift
//  MyMusic
//
//  Created by MacST29 on 2026/06/10.
//

import SwiftUI

struct ContentView: View {
    let soundPlayer = SoundPlayer()
    var body: some View {
        ZStack{
            //背景の画像を指定する
            Image(.background)
            //リサイズする
                .resizable()
            //顔面いっぱいになるようにセーフエリア外まで表示
                .ignoresSafeArea()
            //アスペクト比(縦横比)を維持して短辺基準に収まるようにする
                .scaledToFill()
            //水平にレイアウト
            HStack{
                //シンバルボタン
                Button{
                    //ボタンをタップした時のアクション
                    //シンバルの音を鳴らす
                    soundPlayer.cymbalPlay()
                }label:{
                    //画像を表示
                    Image(.cymbal)
                }//シンバルボタンはここまで
                //ギターボタン
                Button{
                    //ボタンをタップした時のアクション
                }label:{
                    //画像表示
                    Image(.guitar)
                    
                }//ギターボタンはここまで
            }//HStackここまで
        }//Zstackここまで
    }
}

#Preview {
    ContentView()
}

